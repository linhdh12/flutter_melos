name: data
description: Data layer with repositories, data sources, và models.
version: 1.0.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: packages/domain